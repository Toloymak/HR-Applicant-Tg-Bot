@using Microsoft.AspNetCore.Components.Authorization

<AuthorizeView>
    <Authorized>
        @(context.User.Identity?.Name ?? "Unknown")
    </Authorized>
    <NotAuthorized>
        <TgAuth />
    </NotAuthorized>
</AuthorizeView>