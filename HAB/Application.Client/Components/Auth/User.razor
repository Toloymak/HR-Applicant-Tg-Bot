@using Microsoft.AspNetCore.Components.Authorization
@using Shared

<AuthorizeView>
    <Authorized>
        @context.User.GetAlias() (@context.User.Identity?.Name)
    </Authorized>
    <NotAuthorized>
        <TgAuth />
    </NotAuthorized>
</AuthorizeView>